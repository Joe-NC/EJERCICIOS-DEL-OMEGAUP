#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> arreglo(n);
    for (int i = 0; i < n; ++i) {
        cin >> arreglo[i];
    }
    int k;
    cin >> k;
    for (int i = 0; i < n - k; ++i) {
        cout << arreglo[i] << " ";
    }

    cout << endl;

    return 0;
}