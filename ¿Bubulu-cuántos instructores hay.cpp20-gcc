#include <iostream>
#include <sstream>
using namespace std;
int main() {

    string entrada;
    getline(cin, entrada);
    
    istringstream stream(entrada);
    int contador = 0;
    string nombre;
    while (stream >> nombre) {
        contador++;
    }

    cout << contador << endl;

    return 0;
}