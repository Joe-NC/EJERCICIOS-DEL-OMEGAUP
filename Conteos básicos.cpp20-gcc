#include <iostream>
using namespace std;
int main() {

    int A, B;
    cout << "Ingrese dos números enteros (A y B): ";
    cin >> A >> B;

    if (A > B) {
        cout << "Error: A debe ser menor o igual a B." << endl;
        return 1;
    }

    for (int i = A; i <= B; ++i) {
        cout << i << " ";
    }

    cout << endl;

    return 0;
}