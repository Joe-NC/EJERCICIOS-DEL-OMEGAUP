#include <iostream>
using namespace std;
int main() {

    int a, b;
    cout << "Ingrese dos enteros 'a' y 'b' separados por un espacio: ";
    cin >> a >> b;

    for (int i = a; i <= b; ++i) {
        cout << i << " ";
    }

    cout << endl;

    return 0;
}